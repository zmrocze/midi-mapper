[package]
name = "midi-mapper"
version = "0.0.1"
authors = [""]
description = "Remap midi stream"
edition = "2021"

[dependencies]
utils = { path = "../utils" }
tracing = "0.1.37"
midir = "0.10.0"

[dependencies.clap]
version = "4.3.0"
features = ["cargo", "derive"]
